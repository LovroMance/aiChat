// 代理配置测试说明

## Vite 代理配置已完成

### 配置内容：

1. **HTTP API 代理**
   - 前端请求路径：`/api/*`
   - 代理目标：`http://localhost:10086`
   - 重写规则：去掉 `/api` 前缀

2. **WebSocket 代理**
   - 前端请求路径：`/ws/*`
   - 代理目标：`ws://localhost:10086`
   - 支持 WebSocket 协议

### 测试步骤：

1. 启动后端服务（端口 10086）
2. 启动前端开发服务器：`pnpm dev`
3. 访问 http://localhost:5173
4. 检查网络面板中的请求是否正常

### 预期效果：

- API 请求不再出现跨域错误
- WebSocket 连接能够正常建立
- 开发环境和生产环境使用不同的配置策略