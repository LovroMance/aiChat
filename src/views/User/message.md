-- 1. 进入页面后，检查indexedDB中是否有数据
-- Y > 先从indexedDB中获取过往消息数据，然后和服务器数据进行对比看看是否有误
-- 有误就补全，无误就到第二步
-- N > 发送请求获取所有消息数据

-- 2. 发送的消息存到message的仓库store中
-- 通过仓库先进行本地渲染
--

-- 3. 断开连接后，将仓库中的消息数组存到indexedDB中
-- 释放资源
